# 数据产品交互手册

一个零依赖、可直接打开的数据产品组件速查站。内容根据 11 支视频整理，并用通用产品设计术语校准。

包含：

- 8 个清晰分类，每类只保留 4–5 个代表模式
- 每类内置一个循环动画演示，可暂停和播放
- 每个模式只保留使用时机、核心效果和可复制 AI 指令
- “组件 + 触发 + 位置 + 结果”四段式提示词公式
- 简洁响应式布局

直接双击 `index.html`，或在本目录运行：

```bash
python3 -m http.server 8000
```

然后访问 <http://localhost:8000>。
